# GA Tools Examples

if(BUILD_EXAMPLES)
    # Add example executables here when they exist
    # Example:
    # add_executable(ga_example ga_example.cpp)
    # target_link_libraries(ga_example PRIVATE lpzrobots::ga_tools lpzrobots::selforg)
endif()