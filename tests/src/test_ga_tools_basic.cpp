#include <doctest.h>

// Basic smoke test for ga_tools
TEST_CASE("GA Tools basic functionality") {
    SUBCASE("Basic compilation test") {
        // This test ensures that the ga_tools library compiles
        // and basic headers can be included
        CHECK(true); // Simple assertion to verify test framework works
    }
}